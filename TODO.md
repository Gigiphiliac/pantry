- [x] 'Add to shopping list' should strip notes from the ingredient names (e.g. 'eggs, whisked' or 'cooked day old white rice')
- [x] Improve parsing to remove alternative measurements (e.g. '120g/4oz bacon' should detect the '/4oz' and remove it) 
- [x] Rethink alternative ingredients (e.g. 1 cup beer, or beef stock). Perhaps they can be added as a new 'alternative ingredient' field on the Ingredient schema, then on the recipe page, a small button is available to toggle between them. Whichever ingredient is configured at the time of 'add to shopping list' gets added instead of the other. This may require a new parsing method that splits on the word 'or' or the symbol '/'
- [x] tsp and tbsp should be default measurements over 14.79 ml or 187.5 ml...
- [ ] Create a list of non-shopping items such as water, herbs, spices, etc.
- [ ] Adding to shopping list should try to stack with existing items, but fall back to a non-store section. Also add the ability to drag items around and across sections/stores. This would mean that once the 'add to shopping list' is done, the user can move each item into its respective location.
- [ ] Pantry with different sections: never (water, things that are always available), sometimes (herbs, spices, oil, rice... things that buy once and stick around), always (meat, dairy, things that are often needed on a per-recipe basis)
- [ ] Some recipes have ingredients broken into different sections. We should handle this by either adding optional section headers within recipes, or just summing up the ingredients in each section (currently can have two entries of the same ingredient).
- [ ] Allow reordering within shopping list sections. This would mean that once an item is added to a section, it can be moved around within and across sections/stores.
- [ ] Clean up the ingredient instructions. Basically need a more thorough pipeline for each ingredient to separate clearly: {qty, unit (map to existing), ingredient (from existing or new if not found), alternative ingredients, notes (past-tense verb or adjective)}